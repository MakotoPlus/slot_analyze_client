# DRF backend (workspace/config/urls.py -> path("scraping/", include(scraping.urls)))
# docker-compose の slot-analyze サービスは 8083 で公開される（nginx 経由なら 8080）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8083

# true にするとバックエンド無しでダミーデータで動く（ログインは demo / demo）
NEXT_PUBLIC_USE_MOCK=true
